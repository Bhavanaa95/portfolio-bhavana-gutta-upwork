export const metadata = {
  title: "Bhavana Gutta Portfolio",
  description: "AI Researcher and Full-Stack Developer",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
